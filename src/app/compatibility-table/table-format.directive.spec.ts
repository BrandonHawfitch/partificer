import { TableFormatDirective } from './table-format.directive';

describe('TableFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new TableFormatDirective();
    expect(directive).toBeTruthy();
  });
});
